# GRPCEXI

A simple gRPC-based contact management application with a terminal UI built in Go.  
The project demonstrates a clean separation between gRPC services, TUI, and handlers.

## Features

- Create, view, and manage contacts
- Support for multiple phone numbers per contact
- Terminal UI (TUI) with sidebar, forms, and tables
- gRPC client-server communication
